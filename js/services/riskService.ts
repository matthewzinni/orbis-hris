// =========================// RISK SERVICE// =========================import type { Employee } from '../types/employeeTypes';import { evaluateEmployeeFlags } from './employeeFlags';export interface RiskMetricsResult {    totalEmployees: number;    atRiskEmployees: number;    impactPlayers: number;    turnoverRiskPercentage: number;}export function calculateRiskMetrics(    employees: Employee[]): RiskMetricsResult {    const activeEmployees = employees.filter(employee =>        String(employee.status || '').toLowerCase() !== 'terminated'    );    let atRiskEmployees = 0;    let impactPlayers = 0;    activeEmployees.forEach(employee => {        const flags = evaluateEmployeeFlags(employee);        if (flags.atRisk) {            atRiskEmployees++;        }        if (flags.impactPlayer) {            impactPlayers++;        }    });    const turnoverRiskPercentage =        activeEmployees.length > 0            ? Math.round((atRiskEmployees / activeEmployees.length) * 1000) / 10            : 0;    return {        totalEmployees: activeEmployees.length,        atRiskEmployees,        impactPlayers,        turnoverRiskPercentage    };}