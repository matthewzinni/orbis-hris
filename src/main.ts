import '../js/services/supabaseClient'import '../js/utils/helpers'import '../js/core/auth'import '../js/core/permissions'// this is your current app entryimport * as app from '../js/app'// boot confirmationdocument.addEventListener('DOMContentLoaded', () => {    console.log('Orbis booted via main.ts')        // TEMP: expose functions for HTML onclick (bridge old UI)        // Do NOT overwrite existing global functions if they already exist        ; (window as any).signIn = (window as any).signIn || (app as any).signIn        ; (window as any).signOut = (window as any).signOut || (app as any).signOut        ; (window as any).loadAllDashboardData = (window as any).loadAllDashboardData || (app as any).loadAllDashboardData        ; (window as any).openNewEmployeeForm = (window as any).openNewEmployeeForm || (app as any).openNewEmployeeForm        ; (window as any).openCandidatesView = (window as any).openCandidatesView || (app as any).openCandidatesView})